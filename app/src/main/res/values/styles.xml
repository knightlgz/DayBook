<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HomeItem">
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/text_size_home_item</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginBottom">@dimen/margin_vertical_home_item</item>
        <item name="android:layout_marginTop">@dimen/margin_vertical_home_item</item>
        <item name="android:layout_marginStart">@dimen/margin_horizontal_home_item</item>
        <item name="android:layout_marginEnd">@dimen/margin_horizontal_home_item</item>
        <item name="android:clickable">true</item>
    </style>

</resources>
